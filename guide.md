##Reconciliation:

Reconciliation status tells us how it was resolved. MATCHED or PARTIAL_MATCHED.
Transaction status tells us whether the transaction is reconciled.
So, both MATCHED and PARTIAL_MATCH_RESOLVED should result in transaction status MATCHED.